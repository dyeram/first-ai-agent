[project]
name = "first-ai-agent"
version = "1.0.0"
description = "Command-line Python LLM agent with tool calling and a restricted workspace"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
